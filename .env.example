# ===========================================
# DocApp - Plantilla de Variables de Entorno
# ===========================================
# INSTRUCCIONES:
# 1. Copiar este archivo a .env
# 2. Reemplazar los valores con las credenciales de producción
# 3. Asegurar que POSTGRES_* coincida con DB_*

# 1. Configuración de Contenedor DB (Si se usa Docker para la DB)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=cambiar_password_seguro
POSTGRES_DB=docapp_prod_db

# 2. Conexión Backend -> DB
# Si se usa RDS/CloudSQL, colocar aquí los datos de conexión externos.
DB_USER=postgres
DB_PASSWORD=cambiar_password_seguro
DB_DATABASE=docapp_prod_db
DB_HOST=db


# Puertos
DB_PORT=5432
BACKEND_PORT=5000
FRONTEND_PORT=3000

# 3. Seguridad (CRÍTICO: Cambiar en producción)
JWT_SECRET=generar_string_aleatorio_largo_y_seguro

# 4. Servicios Externos
# Gemini AI (Opcional, para análisis de repositorios)
# GEMINI_API_KEY=tu_api_key_aqui
